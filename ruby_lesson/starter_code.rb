class Student
    def initialize()#add attributes here
    end
    
    #write (at least) three methods for the class Student
    def method_1() #at least one method must have an if/elsif/else block
        if
        elsif
        else
        end       
    end

    def method_2(parameter) #at least one must take at one (or more) parameters

    end
    
    def method_3 #at least one method must take no parameters

    end
    
end

if __FILE__ == $0 
    #create a new object in class Student
    #call all of your methods
    #feel free to be as creative as you want!
end
